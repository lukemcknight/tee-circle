# TeeCircle 2.0 Bring-Up Log (Milestone 0, started 2026-07-18)

Every unexpected failure during bring-up gets a row. Fix only what blocks the
exit criteria (owner signs in on hardware 3 ways); everything else is recorded
for Milestone 1+.

| ID | Stage | What happened | Blocking exit? | Resolution/status |
|----|-------|---------------|----------------|-------------------|
| BRING-1 | Simulator test run (Task 3) | UI test `testConvertedLegacyRoundCanRecoverCourseCardAndHandicaps` failed: `failed - Could not reveal UI element '"legacy.handicap.15daa79c-1b33-42b1-aa49-3e8f16f1b9fb" TextField'` (native/TeeCircleUITests/TeeCircleUITests.swift:270, 32.990s) | no | **Resolved** (this commit). Root cause confirmed with screen-recording frame extraction (not just log inference): the 2nd/3rd handicap `TextField`s exist in the hierarchy but are never `isHittable` once the keyboard is up — `LegacyTripSetupView`'s `ScrollView` (native/TeeCircle/Features/Legacy/LegacyTripSetupView.swift) has zero natural scroll slack (all 3 handicap rows + the Finish button already fit the un-keyboarded viewport), and `ScrollViewReader`/manual swipes have no awareness of the keyboard overlay, so once it covers ~45% of the screen no swipe (real or synthesized) or naive `scrollTo` can reveal the later rows — confirmed via video: 10 real synthesized swipes produced zero pixel movement. Fix: reserve keyboard-sized scroll room and jump the handicaps section's own top to the viewport top the moment any handicap field is focused (LegacyTripSetupView.swift:17, 91-92, 100-103, 201), plus a `round.formScroll` accessibility id on the view's own `ScrollView` (line 97) to disambiguate it from the presenting `TripHubView`'s ScrollView, which the `.sheet` presentation keeps queryable underneath. A real user hits the identical wall (default drag-to-reveal starts near the bottom, which is the keyboard), so this was a view fix, not a test fix. UI suite 13/13, full suite 55/55 — first fully green run. |
| BRING-2 | Simulator test run (Task 3) | UI test `testProfileSetupGateBlocksTheAppUntilNameAndUsernameExist` failed: `Failed to get matching snapshot: No matches found for first query match sequence: \`Descendants matching type Any\` -> \`Elements matching predicate '"profileSetup.submit" IN identifiers'\`, given input App element pid: 19446` (native/TeeCircleUITests/TeeCircleUITests.swift:38, 12.391s) | no | **Resolved** in `ebfacfc` ("Fix BRING-2: profile-setup gate test no longer self-submits via keyboard Done"). Root cause was the test, not the app: `-ui-needs-profile` already presented the gate correctly (TeeCircleStore.swift:55-59), but the test called the shared `dismissKeyboard()` helper after the form became valid, which taps the keyboard's Done key — that fires the username field's `onSubmit` (ProfileSetupView.swift:108) and saves the profile immediately, tearing down the gate before the test's own `isEnabled` check and submit tap ran. Fix: stop dismissing via Done at that point; the existing `tap()` helper already retries with a scroll if occluded. |
| BRING-3 | Device §4 email sign-in | OTP email delivered an 8-digit code; app hard-codes 6 digits (AuthenticationView.swift:334 prefix(6); :628-632 rejects count != 6) — code could not be entered. Root cause: project Email OTP Length setting = 8. | yes (email sign-in path) | Fix: dashboard Email provider → OTP Length → 6 (owner). App-side flexible-length input noted for M1 consideration. |
| BRING-4 | Device launch (session restore) | supabase-swift console advisory: initial session emitted after refresh attempt; SDK v3 will always emit stored session and require session.isExpired checks. App explicitly opts for legacy mode (SupabaseSessionService.swift:63-65, emitLocalSessionAsInitialSession: false). No user-visible effect. | no | Advisory only. When supabase-swift major is bumped, add isExpired handling in applyOptionalSession / set emitLocalSessionAsInitialSession true. See supabase-swift PR #822. |
| BRING-5 | Device §4 (trip bootstrap) | get_trip_bootstrap_v1 emitted isCurrentUser: null for unclaimed seats (SQL NULL = comparison); client Bool decode failed with alert. | yes (core trip flow) | Fixed in migration 20260719120000 (coalesce false); regression test added; suite green. Owner must apply to production. |
| BRING-7 | Device §4 Google sign-in | Supabase rejected Google id_token: "Unacceptable audience" naming the web/server client ID (350395562792-4acmgqmq90o…) — provider config missing that ID from accepted Client IDs (only iOS ID entered). | yes (Google path) | Owner: dashboard Auth→Providers→Google→Client IDs = BOTH web and iOS IDs, comma-separated. Config-only. |
| BRING-8 | Device §9 deletion check | Phone was running the bring-up-era build ("Native release" row + pre-M2 deletion stub message) — all M1/M2 client work absent from device. Not a defect. | no | Owner rebuilds from Xcode (native-2 HEAD); re-run §9 checks on the fresh build. |
| BRING-9 | Device §4 Google sign-in (post BRING-7 fix) | Supabase: "Passed nonce and nonce in id_token should either both exist or not" — GoogleSignIn SDK auto-embeds a nonce in the id_token; app passes none to Supabase (TeeCircleStore+Production.swift:101-107, SupabaseSessionService.swift:142-152). Affects all builds. | yes (Google path) | Interim: owner toggles Skip nonce checks ON (dashboard). Real fix: app-generated nonce passed to GIDSignIn and forwarded to Supabase (mirrors Apple flow); then toggle back OFF. |

## Baselines
- Package tests (`swift test --package-path native/Packages/TeeCircleKit`, 2026-07-18): PASS — 32 tests, 0 failures
- Simulator recovery + first completed test run (2026-07-19): Reset steps run — `xcrun simctl shutdown all`, `xcrun simctl erase all`, `killall -9 com.apple.CoreSimulator.CoreSimulatorService`, then `xcrun simctl list devices` — all completed cleanly, no `sudo xcodebuild -runFirstLaunch` needed. Device: **iPhone 16 Pro, iOS 18.6** (no name substitution needed — "iPhone 16 Pro" was available; had to pin `OS=18.6` explicitly in the `-destination` string because the bare `name=iPhone 16 Pro` spec resolves `OS:latest` → 26.3.1, a runtime this device isn't paired with, causing "Unable to find a device matching the provided destination specifier"). Build: `xcodebuild ... -derivedDataPath native/DerivedData build` → **BUILD SUCCEEDED**. Test: `xcodebuild ... -derivedDataPath native/DerivedData test` → run **COMPLETED** (no CoreSimulator crash, no hang) — first-ever completed test run of this app. Result: **55 tests, 53 passed, 2 failed, 0 skipped** (42 unit tests in TeeCircleTests all passed in 0.367s; 13 UI tests in TeeCircleUITests, 11 passed / 2 failed, in 274.271s). Failures recorded as BRING-1 and BRING-2 above; both are UI-element-lookup failures mid-flow, not launch/crash failures. xcresult bundle: `native/DerivedData/Logs/Test/Test-TeeCircle-2026.07.19_00-02-52--0400.xcresult`.
- Supabase config (§3, owner-executed 2026-07-19): Apple+Google providers confirmed enabled via public /auth/v1/settings (apple:true google:true email:true). Magic-link template + native_writes_enabled applied by owner in dashboard; final proof lands with §4 (code in email, username claim saves).
- Product decision (owner, 2026-07-19): live scoring is FREE for the lean debut — `purchases_required` runtime flag set to false in production (SQL editor). This is the intended prod state through the 2.0.0 release; M3's kill-switch verification must expect purchases_required=false (paywall hidden, nothing charges), NOT revert it. Reversible when monetization work resumes.
- BRING-6 (2026-07-19, expected-dark surface, not a defect): owner hit the public Follow-Live web page (website LiveTrip.tsx) which shows "We can't find this leaderboard" because `public_previews_enabled` is off (resolve_public_trip_preview returns null, services.sql:459). Enabling it requires: flag on + snapshot worker pipeline (enqueue_missing_trip_snapshot_jobs / recompute-trip-snapshot-v1 cron + worker secret, runbook §5 line 78 unchecked) + Vercel env (PUBLIC_PREVIEW_ENDPOINT, proxy secret). In-app leaderboard reads the same snapshots. Scoped to Milestone 1.
- Full suite fully green (2026-07-20, Task 4): `xcodebuild ... -derivedDataPath native/DerivedData test` (no `-only-testing`) → **55 tests, 55 passed, 0 failed, 0 skipped** — first fully green run of the whole suite. Closes out BRING-1 and BRING-2 above. xcresult bundle: `native/DerivedData/Logs/Test/Test-TeeCircle-2026.07.20_12-58-42--0400.xcresult`.
