# Generate two independent, random values of at least 32 bytes. Configure the
# proxy secret with the same value on Vercel; keep both values server-side.
PUBLIC_PREVIEW_PROXY_SECRET=replace-with-a-random-server-only-secret
PUBLIC_RATE_LIMIT_SALT=replace-with-an-independent-random-secret
